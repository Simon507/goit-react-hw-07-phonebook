//але оскільки проект маленький - можна було б прописати у
// аргументах юзселекторів у Контактлисті

export const getContactList = state => state.contactList.contactList;

export const getFindTarget = state => state.filters.findTarget;
